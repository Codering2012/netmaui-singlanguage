<RULE[user_global]>
NEVER execute `git checkout` to restore files or discard changes.
If an accidental file deletion or unintended change occurs, IMMEDIATELY stop execution and ask the user to revert the changes (leveraging Antigravity's built-in change revert features).
</RULE[user_global]>
