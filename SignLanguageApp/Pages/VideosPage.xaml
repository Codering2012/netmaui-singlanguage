<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SignLanguageApp.ViewModels"
             xmlns:helpers="clr-namespace:SignLanguageApp.Helpers"
             x:Class="SignLanguageApp.Pages.VideosPage"
             x:DataType="vm:VideoViewModel"
             Shell.NavBarIsVisible="False"
             Title="Videos"
             BackgroundColor="{StaticResource ColorBackgroundDark}">

    <ContentPage.Resources>
        <helpers:StringIsNullOrEmptyConverter x:Key="StringIsNullOrEmptyConverter" />
        <helpers:CategoryColorConverter x:Key="CategoryColorConverter" />
        <helpers:CategoryToEmojiConverter x:Key="CategoryToEmojiConverter" />
        <helpers:BoolToColorConverter x:Key="BoolToColorConverter" />
        <helpers:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    </ContentPage.Resources>

    <Grid>
        <ScrollView>
            <VerticalStackLayout Spacing="16" Padding="16,10,16,24">

                <Border StrokeThickness="0" Background="#123247" StrokeShape="RoundRectangle 20" Padding="18">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Video library" FontSize="28" FontAttributes="Bold" TextColor="White" />
                        <Label Text="Watch short explainers and full lesson walkthroughs" FontSize="13" TextColor="#BBD5E8" />
                    </VerticalStackLayout>
                </Border>

                <Border StrokeThickness="0" Background="#10283B" StrokeShape="RoundRectangle 14" Padding="12">
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                        <Entry Grid.Column="0"
                               Text="{Binding SearchQuery}"
                               Placeholder="Search by title or description"
                               PlaceholderColor="#87A7BE"
                               TextColor="White"
                               BackgroundColor="Transparent"
                               ReturnType="Search" />
                        <Button Grid.Column="1"
                                Text="Search"
                                Command="{Binding SearchVideosCommand}"
                                BackgroundColor="{StaticResource Primary}"
                                TextColor="White"
                                FontAttributes="Bold"
                                FontSize="12"
                                Padding="14,8" />
                    </Grid>
                </Border>

                <VerticalStackLayout Spacing="8">
                    <Label Text="Categories" FontSize="14" FontAttributes="Bold" TextColor="White" />
                    <CollectionView ItemsSource="{Binding Categories}" SelectionMode="Single" SelectedItem="{Binding SelectedCategory}">
                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout Orientation="Horizontal" ItemSpacing="8" />
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <Border StrokeThickness="0" Background="#0D2133" StrokeShape="RoundRectangle 999" Padding="12,7">
                                    <HorizontalStackLayout Spacing="6">
                                        <Label Text="{Binding ., Converter={StaticResource CategoryToEmojiConverter}}" FontSize="10" TextColor="#A1C1D6" VerticalOptions="Center" />
                                        <Label Text="{Binding .}" FontSize="12" FontAttributes="Bold" TextColor="White" VerticalOptions="Center" />
                                    </HorizontalStackLayout>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>

                <Label Text="{Binding TotalVideos, StringFormat='Results: {0}'}" FontSize="12" TextColor="#8FAEC4" />

                <CollectionView ItemsSource="{Binding Videos}" SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="vm:VideoItem">
                            <Border StrokeThickness="0" Background="#0E2133" StrokeShape="RoundRectangle 14" Padding="12" Margin="0,0,0,10">
                                <VerticalStackLayout Spacing="10">
                                    <Border StrokeThickness="0" Background="#17324B" StrokeShape="RoundRectangle 10" HeightRequest="170">
                                        <Grid>
                                            <Image Source="{Binding ThumbnailUrl}" Aspect="AspectFill" />
                                            <BoxView BackgroundColor="#60000000" />
                                            <Label Text="PLAY" HorizontalOptions="Center" VerticalOptions="Center" FontAttributes="Bold" FontSize="14" TextColor="White" />
                                            <Border HorizontalOptions="End" VerticalOptions="End" Margin="8" Padding="8,4" StrokeThickness="0" Background="#AA000000" StrokeShape="RoundRectangle 6">
                                                <Label Text="{Binding DurationSeconds, StringFormat='{0:F0}s'}" FontSize="10" TextColor="White" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <VerticalStackLayout Spacing="3">
                                        <Label Text="{Binding Title}" FontSize="14" FontAttributes="Bold" TextColor="White" MaxLines="2" LineBreakMode="TailTruncation" />
                                        <Label Text="{Binding Description}" FontSize="12" TextColor="#8EAFc5" MaxLines="2" LineBreakMode="TailTruncation" />
                                        <Grid ColumnDefinitions="Auto,Auto,*" ColumnSpacing="8">
                                            <Label Grid.Column="0" Text="{Binding Category}" FontSize="11" TextColor="{StaticResource Secondary}" />
                                            <Label Grid.Column="1" Text="|" FontSize="11" TextColor="#799BB3" />
                                            <Label Grid.Column="2" Text="{Binding ViewCount, StringFormat='{0:N0} views'}" FontSize="11" TextColor="#799BB3" />
                                        </Grid>
                                    </VerticalStackLayout>

                                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8">
                                        <Button Grid.Column="0"
                                                Text="Watch"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.WatchVideoCommand}"
                                                CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource Primary}"
                                                TextColor="White"
                                                FontSize="11"
                                                FontAttributes="Bold"
                                                Padding="10,6" />
                                        <Button Grid.Column="1"
                                                Text="Like"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.LikeVideoCommand}"
                                                CommandParameter="{Binding .}"
                                                BackgroundColor="{Binding IsLiked, Converter={StaticResource BoolToColorConverter}}"
                                                TextColor="White"
                                                FontSize="11"
                                                FontAttributes="Bold"
                                                Padding="10,6" />
                                        <Button Grid.Column="2"
                                                Text="Share"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ShareVideoCommand}"
                                                CommandParameter="{Binding .}"
                                                BackgroundColor="#1E3A53"
                                                TextColor="White"
                                                FontSize="11"
                                                FontAttributes="Bold"
                                                Padding="10,6" />
                                    </Grid>
                                </VerticalStackLayout>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Border IsVisible="{Binding TotalVideos, Converter={StaticResource CountToVisibilityConverter}}"
                        StrokeThickness="0"
                        Background="#0F2031"
                        StrokeShape="RoundRectangle 14"
                        Padding="18">
                    <VerticalStackLayout Spacing="6" HorizontalOptions="Center">
                        <Label Text="No videos found" FontSize="16" FontAttributes="Bold" TextColor="White" HorizontalOptions="Center" />
                        <Label Text="Try another search or category" FontSize="12" TextColor="#8DAEC4" HorizontalOptions="Center" />
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

        <ActivityIndicator IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           Color="{StaticResource Primary}"
                           Scale="1.3" />
    </Grid>
</ContentPage>
