# =====================================================================================
# KAGGLE AUTOMATION SCRIPT
# Copy the contents below into a single Kaggle Notebook cell and run it.
# It handles moving the files, verifying the GPUs, and starting the training.
# =====================================================================================

# 1. Copy the entire 'Kaggle script' folder from your uploaded dataset into Kaggle's working directory.
# (Replace 'your-uploaded-dataset-name' with the actual name of your script dataset in Kaggle)
!cp -r /kaggle/input/your-uploaded-dataset-name/* /kaggle/working/

# 2. Change directory into the train folder where our new script is located
%cd /kaggle/working/train

# 3. (Optional) Verify the dual T4 GPUs are visible to PyTorch
import torch
print("CUDA Available:", torch.cuda.is_available())
print("Device Count:", torch.cuda.device_count())

# 4. Start the training! 
# (Make sure to replace the --data-dir with your actual preprocessed dataset path)
!python run_kaggle_2x_t4.py \
    --data-dir "/kaggle/input/asl-preprocessed-phase1" \
    --epochs 50 \
    --batch-size 128 \
    --lr 0.001 \
    --save-dir "/kaggle/working/checkpoints"
